package first;

//计算数组中最大的子序列和

public class first {
	
}

class Solution {
	public int MaxArray(int a[],int dp,int index) {
		if(index>=a.length)
			return dp;
		
		int max=dp;//最大值，dp是前序列的最大值
		dp=max+a[index];
		return Math.max(max,(MaxArray(a, dp, index+1)));
	}
}