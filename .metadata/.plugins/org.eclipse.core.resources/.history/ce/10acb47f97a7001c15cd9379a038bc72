package quicksort;

public class quicksort<T extends Comparable<T>> {
	public T[] quicksort(T[] date) {
		return quicksort(T[] date,0,date.length-1);
	}
	public T[] quicksort(T[] date,int left,int right) {
		
	}
}
