package first;

//计算数组中最大的子序列和

public class first {
	
}

class Solution {
	public int MaxArray(int a[],int dp,int index) {
		if(index>=a.length)
			return dp;
		
		int max=dp=dp=max+a[index];
		return Math.max(dp,MaxArray(a, dp, index+1));
	}
}